<Window x:Class="POS.UI.FondoInicialWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Fondo inicial" Width="420" Height="200"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource BrushFondo}">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Ingresa el fondo inicial (efectivo):" FontWeight="Bold" Margin="0,0,0,8"/>

        <TextBox x:Name="TxtMonto" Grid.Row="1" FontSize="16"
                 VerticalContentAlignment="Center" Padding="6"
                 HorizontalAlignment="Stretch" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Cancelar" Width="100" Margin="0,0,8,0" Click="Cancelar_Click"/>
            <Button Content="Abrir turno" Width="120" Click="AbrirTurno_Click" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>