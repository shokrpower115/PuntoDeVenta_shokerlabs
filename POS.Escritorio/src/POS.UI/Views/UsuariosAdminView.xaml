<UserControl x:Class="POS.UI.Views.UsuariosAdminView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="320"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Margin="0,0,15,0">
            <Button Content="← Volver" Command="{Binding VolverCommand}" Style="{StaticResource BotonPrimario}"
                    HorizontalAlignment="Left" Padding="15,5" Margin="0,0,0,15"/>

            <TextBlock Text="Nuevo usuario" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

            <TextBlock Text="Nombre de usuario (login):" Margin="0,5,0,3"/>
            <TextBox Text="{Binding NuevoNombreUsuario, UpdateSourceTrigger=PropertyChanged}" Padding="5"/>

            <TextBlock Text="Contraseña:" Margin="0,10,0,3"/>
            <TextBox Text="{Binding NuevoPassword, UpdateSourceTrigger=PropertyChanged}" Padding="5"/>

            <TextBlock Text="Nombre completo:" Margin="0,10,0,3"/>
            <TextBox Text="{Binding NuevoNombreCompleto, UpdateSourceTrigger=PropertyChanged}" Padding="5"/>

            <TextBlock Text="Rol:" Margin="0,10,0,3"/>
            <ComboBox ItemsSource="{Binding RolesDisponibles}" SelectedItem="{Binding NuevoRol}" Padding="5"/>

            <TextBlock Text="Sucursal:" Margin="0,10,0,3"/>
            <ComboBox ItemsSource="{Binding Sucursales}" SelectedValuePath="Key" DisplayMemberPath="Value"
                      SelectedValue="{Binding NuevaSucursalId}" Padding="5"/>

            <Button Content="Crear usuario" Command="{Binding CrearUsuarioCommand}" Style="{StaticResource BotonAccento}"
                    Height="40" Margin="0,15,0,0"/>

            <TextBlock Text="{Binding Mensaje}" Foreground="DarkOrange" Margin="0,10,0,0" TextWrapping="Wrap"/>
        </StackPanel>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Usuarios existentes" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

            <DataGrid Grid.Row="1" ItemsSource="{Binding Usuarios}" AutoGenerateColumns="False" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Usuario" Binding="{Binding NombreUsuario}" Width="*"/>
                    <DataGridTextColumn Header="Nombre" Binding="{Binding NombreCompleto}" Width="*"/>
                    <DataGridTextColumn Header="Rol" Binding="{Binding Rol}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</UserControl>